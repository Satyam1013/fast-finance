# Render Blueprint — https://render.com/docs/blueprint-spec
#
# Deploys the API from the `develop` branch. MongoDB is NOT provisioned here
# (Render has no managed MongoDB) — create a free MongoDB Atlas cluster and set
# MONGODB_URI in the Render dashboard.
#
# First deploy:
#   1. Render Dashboard → New → Blueprint → pick this repo, branch `develop`.
#   2. Fill the `sync: false` vars when prompted (MONGODB_URI, ALLOWED_ORIGINS,
#      PUBLIC_ASSET_BASE_URL — the last is https://<service>.onrender.com/api/v1/files).
#   3. After it's live, run the seed once from a local shell pointed at the same
#      MONGODB_URI:  npm run seed
services:
  - type: web
    name: fast-finance-api
    runtime: node
    plan: free
    branch: develop
    autoDeploy: true
    # --include=dev: nest build needs devDependencies even with NODE_ENV=production
    buildCommand: npm ci --include=dev && npm run build
    startCommand: npm run start:prod
    healthCheckPath: /api/v1/health
    envVars:
      - key: NODE_VERSION
        value: "22"
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "10000" # Render overrides this with its own port; main.ts reads process.env.PORT

      # ── Database — set in dashboard (MongoDB Atlas connection string) ──
      - key: MONGODB_URI
        sync: false

      # ── Auth ──
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_ACCESS_TTL
        value: 15m
      - key: JWT_REFRESH_TTL_DAYS
        value: "30"
      - key: BCRYPT_ROUNDS
        value: "12"

      # CORS — comma-separated origins of the PWA / admin panel
      - key: ALLOWED_ORIGINS
        sync: false

      # ── OTP ──
      # Keep dev mode on until MacroPage Connect creds are set, then flip
      # OTP_DEV_MODE=false (OTP_CHANNEL is already whatsapp) and add the
      # MACROPAGE_CONNECT_* vars in the dashboard.
      - key: OTP_DEV_MODE
        value: "true"
      - key: OTP_DEV_CODE
        value: "0000"
      - key: OTP_LENGTH
        value: "4"
      - key: OTP_TTL_SECONDS
        value: "300"
      - key: OTP_CHANNEL
        value: whatsapp
      - key: MACROPAGE_CONNECT_BASE_URL
        sync: false
      - key: MACROPAGE_CONNECT_API_KEY
        sync: false
      - key: MACROPAGE_CONNECT_OTP_TEMPLATE
        sync: false
      - key: MACROPAGE_CONNECT_SENDER
        sync: false

      # ── File storage ──
      # local disk is EPHEMERAL on Render (lost on every redeploy). Set
      # STORAGE_DRIVER=s3 and fill the S3_* vars (DigitalOcean Spaces) in the
      # dashboard for anything that must persist.
      - key: STORAGE_DRIVER
        value: local
      - key: PUBLIC_ASSET_BASE_URL
        sync: false
      - key: S3_ENDPOINT
        sync: false
      - key: S3_REGION
        sync: false
      - key: S3_BUCKET
        sync: false
      - key: S3_ACCESS_KEY
        sync: false
      - key: S3_SECRET_KEY
        sync: false

      # ── Feature flags / misc ──
      - key: FEATURE_ACCOUNT_AGGREGATOR
        value: "false"
      - key: GST_RATE_PERCENT
        value: "18"
      - key: SUPPORT_PHONE
        sync: false
      - key: SUPPORT_RESPONSE_LABEL
        value: Our team responds within 5 minutes
